# V0.3 搜题做题 + 手把手教学 — 开发大纲

> 基于 V0.2（做题系统 + 单用户小题库），做核心学习闭环。
> 参考日期：2026-07-05
> 状态：待开发

---

## 一、总览

```
试卷图片（远期）
   ↓ 多模态 OCR + 切题 + 解法打标 + 教师路由（远期）
输入层
   ↓ 并发调 Solver（远期多题并行）
做题系统（V0.1-0.2 已有，基本不变）
   ├─ Solver（解答）
   ├─ Verifier（校验 + 干扰项生成 ← 新增）
   └─ Formatter（格式化 + 入库）
   ↓
教学层（V0.3 核心）
搜题做题 → 手把手教学 → 错因沉淀
   ↓
出题系统（V0.4）
```

---

## 二、V0.3 改动清单

### 2.1 Verifier 增强（后端，solver.py）

**目标：** 每个步骤增加干扰项，供手把手教学当选择题用。

每个步骤的 output 增加 `distractors` 字段：

```json
{
  "step_number": 2,
  "title": "求对称轴",
  "standard_writing": "对称轴 x = -b/2a = -a",
  "knowledge_point": "二次函数对称轴公式",
  "distractors": [
    {"option": "A", "text": "x = -a",            "correct": true,  "mistake_type": null},
    {"option": "B", "text": "x = a",             "correct": false, "mistake_type": "符号错误", "mistake_desc": "负号漏掉了"},
    {"option": "C", "text": "x = -b/a",          "correct": false, "mistake_type": "知识性错误", "mistake_desc": "对称轴公式记成了 -b/a"},
    {"option": "D", "text": "x = -2a",           "correct": false, "mistake_type": "计算错误", "mistake_desc": "代入时把 2a 当成了 a"}
  ]
}
```

改动：
- 修改 Verifier 的每个板块 prompt，在每步输出中增加干扰项生成要求
- 后端增加 `_parse_distractors()` 解析函数
- 兼容：旧数据没有 distractor 字段的教学流程处理（兜底：展示标准写法，让学生自己确认）

### 2.2 小题库扩展（后端，database.py）

错因记录接入小题库：

- `practice_records` 表增加 `step_errors` 字段（JSON，记录每步的选项和错因）
- `save_practice_record()` — 记录一次教学交互结果
- `get_practice_by_question()` — 查询某道题的历史练习记录

### 2.3 后端教学 API（后端，main.py）

新增接口：

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/teach/start` | 开始教学：调做题系统获取分步+干扰项数据 |
| POST | `/teach/check` | 提交学生选项，返回正确/错误+错因+下一步 |
| POST | `/teach/complete` | 教学完成，将错因记录写入小题库 |

### 2.4 搜题做题页（前端，新文件）

已有前端的升级版，独立页面：

- 标题：搜题做题
- 搜索框 + 题型筛选
- 结果展示：题目、分步答案（折叠）、知识点标签、难度雷达图
- 底部按钮：[手把手教我做] [出同类题]
- 四老师选择保留

### 2.5 手把手教学页（前端，新页面）

核心交互页面。

**主流程（选择题模式）：**

- 顶部：返回 + 标题 + 进度 + 计时器
- 逐轮展示：
  - 当前步骤的提示
  - 四个选项按钮（点击高亮，确认提交）
  - 正确 → 绿色反馈，自动下一步
  - 错误 → 红色反馈，展示 AI 预期错因 + 补充框

**多路径处理（E. 我不是这么做的）：**

```
步骤2：求对称轴

A. x = -a             ← 配方法，正确
B. x = a              ← 符号反了
C. x = -b/a           ← 公式记错
D. x = -2a            ← 代入错误
E. 我不是这么做的     ← 走自由输入

学生选 E → 跳出文字框："我是用配方法做的……"
AI 判断对错 → 若对，锚定到步骤2终点 → 进入步骤3
```

- 比对逻辑：直接匹配 distractors 的 `correct` 字段，零 AI 调用
- E 场景才调 AI，但只做单步判断，小模型也够

**完成页：**

```
🎉 这道题你做完了！
  用时：4分23秒
  正确步数：4/5
  错因记录：符号错误×1

  [出几道同类题巩固]（V0.4）
  [做下一道]
```

---

## 三、远期架构（V1.0+，不在此版本实现）

记录讨论过的设计方向，留作参考。

### 3.1 多模态输入层

```
试卷图片
  → 多模态 API（OCR + 切题 + 解法识别 + 教师路由）
     → 题1：选择题，简单 → 路由梁梁（flash）
     → 题2：立体几何，小问1[建系法] + 小问2[几何法]
     → 题3：压轴题，新定义 → 路由雪峰（pro）
         ↓
     并发调 Solver（每道题独立，DS 并发上限 500 不设限）
         ↓
     各自走 Verifier → Formatter
         ↓
     前端展示（每道题独立卡片）
```

- 切题和判难度提前到输入层
- 一题多解法：打标后原题传给 Solver，靠 Solver 已有的多小问能力消化
- 后端和数据库完全复用，只需新增 iPadOS 前端

### 3.2 Solver 按板块拆分（输入层就绪后可做）

Solver prompt 不再一块通吃，改成按板块拆：

```
prompts/solver/
  ├── 解析几何.md
  ├── 立体几何_建系法.md
  ├── 立体几何_几何法.md
  ├── 数列.md
  ├── 函数与导数.md
  ├── 三角函数与解三角形.md
  ├── 概率与统计.md
  ├── 非常规压轴题.md
  ├── 选择题.md
  └── 填空题.md
```

每个 prompt 只干一件事，长度减半，调一个不影响另一个。与 Verifier 的 prompt 目录结构对称。

### 3.3 填空/手写交互（远期）

```
交互模式演进：
  V0.3  选择题（点击选项，零延迟比对）       ← 立刻能做
  V0.4  出题系统巩固                         ← 基于错因
  V1.0+ 填空题交互（学生写 → OCR → AI 比对 + 确认环节）
```

### 3.4 手写 OCR 方案

| 场景 | 方案 | 备注 |
|------|------|------|
| 单步输入（手把手教学） | 在线多模态模型（DeepSeek / Gemini） | 可实时转换，学生确认 |
| 全局做题（录屏分析） | 同上 + 上下文对齐 | 依托做题系统分步结构做映射校正 |
| OCR 识别不到的错别字 | 不影响"算没算对"的判断 | 做题系统的分步数据提供参照系 |

学生确认环节不可省。另：OCR 识别结果与做题系统的标准步骤对齐时，靠时间戳和字符轮廓做匹配，不需要学生手动标注。卡点检测：某步 OCR 长时间无新内容 → 明确的卡点信号。

---

## 四、开发顺序

| 顺序 | 内容 | 说明 |
|------|------|------|
| 1 | 修改 Verifier prompt + 解析函数 | 让每步输出带上干扰项 |
| 2 | `/teach/start` + `/teach/check` + `/teach/complete` 三个 API | 教学后端逻辑 |
| 3 | 手把手教学前端页 | 选择题交互 + 错因展示 + 补充框 + 多路径处理 |
| 4 | 搜题做题前端页 | 搜索 + 展示 + 跳转 |
| 5 | 小题库交互记录写入 | 错因数据沉淀 |
| 6 | 调试 + 兼容旧数据兜底 | 跑通全链路 |

---

## 五、设计决策记录

| 决策 | 结论 | 原因 |
|------|------|------|
| 干扰项由谁生成 | Verifier | 天然分步，prompt 已分类，幻觉风险低 |
| 学生作答比对方式 | 匹配 correct 字段，不调 AI | 零延迟，零成本，零幻觉 |
| 选错后怎么做 | 展示 AI 预期错因 + 可选打字补充 | 补充框收集真实错因，反哺数据 |
| 一题多解怎么处理 | 选项加 E. 我不是这么做的 → 自由输入 → AI 锚定到步骤终点 | 不阻塞主流程，多路径推迟到比对环节 |
| Solver prompt 拆分时机 | 等输入层就绪后做 | 当前 Solver 一体够用 |
| 多题并行 | 并发调 Solver，不设限流 | DS 并发上限 500，用不到 |
| 干扰项数据存储 | 随 answer_json 一起存小题库 | 复用现有字段，不改表结构 |
| 教学记录 | 新增 step_errors 字段 | 需要结构化查询，独立存更干净 |
| 远期 OCR | 在线多模态模型，前端让用户选 | 零新增成本，DeepSeek 已接入 |
