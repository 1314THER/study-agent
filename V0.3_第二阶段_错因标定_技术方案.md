# V0.3 第二阶段 — 错因标定系统

> 范围：错因数据库 + 搜题做题页标记入口 + 个人题库错因展示
> 目的：先沉淀真实错因数据，为后续手把手教学（V0.3 第三阶段）铺路
> 不动 prompts、不动 Verifier、不动三阶管线
> 后端新增约 30 行，前端两页各改动约 60 行

---

## 一、总览

```
现有管线（不变）                    Phase 2（本版本）
                                ┌─────────────────────────┐
用户搜题 → 三步管线 → 展示     │  手把手教学（下一版本）  │
           ↓                 └─────────┬───────────────┘
        Phase 1（本版本）               ↑
    展示结果 + 每步可标记错因 ──────────┘
           ↓                     复用 step_errors 数据
    数据落库 → 个人题库红标展示
```

本版本只做"标记"和"展示"两个动作，不做自动化比对。

---

## 二、数据库变更

### 2.1 新增 `step_errors` 表

```sql
CREATE TABLE IF NOT EXISTS step_errors (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    question_id INTEGER NOT NULL REFERENCES questions(id),
    step_number INTEGER NOT NULL,        -- 哪一步（1-based）
    chunk_id INTEGER NOT NULL,           -- 属于哪个 chunk（对应 chunk_results 中的 chunk_id）
    mistake_type TEXT NOT NULL,           -- 错因分类
    mistake_detail TEXT,                  -- 学生手动输入的具体说明
    student_input TEXT,                   -- 学生当时输入的原话（Phase 3 用，Phase 1 暂为空）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_step_errors_qid ON step_errors(question_id);
CREATE INDEX IF NOT EXISTS idx_step_errors_type ON step_errors(mistake_type);
```

### 2.2 `mistake_type` 枚举

仅接受以下七类（前端选择器也照此列出）：

| 类型 | 含义 |
|------|------|
| `符号错误` | 正负号、不等号方向等 |
| `计算错误` | 四则运算、代入数值计算错误 |
| `公式记错` | 公式/定理记混或记错 |
| `知识性错误` | 概念理解偏差 |
| `审题错误` | 漏看条件 / 理解错题意 |
| `思路错误` | 解题方向不对 |
| `其他` | 无法归入以上分类 |

后端入库时校验：`mistake_type` 必须属于以上七类，否则拒绝写入。

### 2.3 `questions` 表增加来源字段

`questions` 表新增两列：

```sql
ALTER TABLE questions ADD COLUMN source_type TEXT;
ALTER TABLE questions ADD COLUMN source_meta TEXT;
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `source_type` | TEXT | 题目来源枚举（见下方） |
| `source_meta` | TEXT | JSON，存放来源详细信息 |

#### `source_type` 枚举

| 值 | 含义 | `source_meta` 示例 |
|----|------|---------------------|
| `ai_generated` | AI 自行出题 | `{"prompt": "生成一道函数与导数的压轴题"}` |
| `human` | 人工录入（无明确卷子来源） | `{}` |
| `exam_paper` | 来自某张标准试卷 | `{"exam": "2024年高考数学全国I卷", "question_number": "17", "section": "解答题", "total_score": 12}` |
| `web_search` | 从网页/搜索引擎抓取 | `{"url": "https://...", "title": "题目标题", "site": "知乎"}` |
| `exam_ocr` | 整卷 OCR 识别后切题 | `{"exam": "2025年海淀一模", "source_file": "/scans/haidian-2025-03.pdf", "page": 3, "ocr_engine": "deepseek-v4"}` |

#### 设计说明

- `source_type` 是硬编码枚举，后端校验入库
- `source_meta` 是自由 JSON，不在数据库层做 schema 约束，由各管线自行决定写入内容
- 扩展性：后续增加新的来源类型（如 `textbook`、`competition`）只需加一个枚举值，不动表结构
- 迁移兼容：已有旧题的 `source_type` 和 `source_meta` 均为 NULL，前端展示时按"来源未知"处理

### 2.4 不需要改动的表

`mistakes` 表、`practice_records` 表全部不动。

---

## 三、后端 API

### 3.1 `POST /questions/{qid}/step-error` — 记录错因

```python
class StepErrorRequest(BaseModel):
    step_number: int = Field(..., ge=1, description="步骤编号")
    chunk_id: int = Field(..., ge=1, description="块编号")
    mistake_type: str = Field(..., description="错因类型")
    mistake_detail: str = Field("", description="错因详细说明")

_VALID_MISTAKE_TYPES = {"符号错误", "计算错误", "公式记错",
                        "知识性错误", "审题错误", "思路错误", "其他"}

@app.post("/questions/{qid}/step-error")
def api_add_step_error(qid: int, req: StepErrorRequest):
    """记录某题某步的错因"""
    if req.mistake_type not in _VALID_MISTAKE_TYPES:
        return {"error": "invalid_mistake_type",
                "valid_types": list(_VALID_MISTAKE_TYPES)}
    from backend.database import add_step_error
    try:
        error_id = add_step_error(
            question_id=qid,
            step_number=req.step_number,
            chunk_id=req.chunk_id,
            mistake_type=req.mistake_type,
            mistake_detail=req.mistake_detail
        )
        return {"id": error_id, "saved": True}
    except Exception as e:
        return {"error": str(e), "saved": False}
```

### 3.2 `DELETE /questions/{qid}/step-error/{eid}` — 删除错因

```python
@app.delete("/questions/{qid}/step-error/{eid}")
def api_delete_step_error(qid: int, eid: int):
    """删除一条错因记录"""
    from backend.database import delete_step_error
    ok = delete_step_error(eid, qid)
    if not ok:
        return {"deleted": False, "message": "未找到该错因记录"}
    return {"deleted": True, "id": eid}
```

### 3.3 现有 API 修改：`GET /questions/{qid}` 增加错因字段

```python
@app.get("/questions/{qid}")
def get_question(qid: int):
    """返回单题完整记录（含 answer_json + step_errors）"""
    from backend.database import get_question_by_id, get_step_errors
    result = get_question_by_id(qid)
    if result is None:
        return {"error": "not_found", "id": qid}
    # 新增：附带该题所有错因记录
    result["step_errors"] = get_step_errors(qid)
    return result
```

### 3.4 新增：`GET /questions/errors` — 有错因的题目列表

```python
@app.get("/questions/errors")
def api_questions_with_errors():
    """列出所有有错因标记的题目（摘要信息）"""
    from backend.database import get_questions_with_errors
    return get_questions_with_errors()
```

---

## 四、数据库函数（backend/database.py 新增 ~40 行）

```python
def add_step_error(question_id: int, step_number: int, chunk_id: int,
                   mistake_type: str, mistake_detail: str = "") -> int:
    """写入一条错因记录，返回 id"""
    conn = get_connection()
    cur = conn.execute(
        """INSERT INTO step_errors
           (question_id, step_number, chunk_id, mistake_type, mistake_detail)
           VALUES (?, ?, ?, ?, ?)""",
        (question_id, step_number, chunk_id, mistake_type, mistake_detail)
    )
    conn.commit()
    eid = cur.lastrowid
    conn.close()
    return eid


def get_step_errors(question_id: int) -> list:
    """返回某题的所有错因记录（按步骤排序）"""
    conn = get_connection()
    rows = conn.execute(
        """SELECT id, step_number, chunk_id, mistake_type, mistake_detail, created_at
           FROM step_errors WHERE question_id = ?
           ORDER BY chunk_id, step_number""",
        (question_id,)
    ).fetchall()
    conn.close()
    return [dict(r) for r in rows]


def delete_step_error(error_id: int, question_id: int) -> bool:
    """删除一条错因记录"""
    conn = get_connection()
    cur = conn.execute(
        "DELETE FROM step_errors WHERE id = ? AND question_id = ?",
        (error_id, question_id)
    )
    deleted = cur.rowcount > 0
    conn.commit()
    conn.close()
    return deleted


def get_questions_with_errors() -> list:
    """返回至少有一条错因记录的题目列表"""
    conn = get_connection()
    rows = conn.execute(
        """SELECT DISTINCT q.id, q.content, q.category_level1, q.category_level2,
                  q.difficulty_level, q.difficulty_score, q.question_type, q.created_at
           FROM questions q
           INNER JOIN step_errors e ON e.question_id = q.id
           ORDER BY q.created_at DESC"""
    ).fetchall()
    conn.close()
    return [dict(r) for r in rows]
```

---

## 五、前端改动 — index.html（和老师对答案）

### 5.1 每步卡片增加"标记错因"按钮

在现有的步骤渲染中，每个 step 末尾增加一个错因标记区：

```javascript
// 在 displayResult() 的步骤循环中，每步末尾追加：
html += '<div class="step-error-btn" onclick="showErrorForm(qid, ' + step.step_number + ', ' + chunkId + ', this)">';
html += '  <span class="error-flag-icon">⚠️</span>';
html += '  <span>标记错因</span>';
html += '</div>';
if (step.hasError) {
    html += '<div class="step-error-badge">已标记</div>';
}
```

### 5.2 错因标记弹窗（浮层）

点击"标记错因"后弹出 overlay，不跳转页面：

```
┌─────────────────────────────────┐
│  标记错因 — 步骤3               │
│                                 │
│  错因类型：                     │
│  [符号错误 ▼]                   │
│  ┌─ 下拉选项 ───────────────┐  │
│  │ 符号错误                   │  │
│  │ 计算错误                   │  │
│  │ 公式记错                   │  │
│  │ 知识性错误                 │  │
│  │ 审题错误                   │  │
│  │ 思路错误                   │  │
│  │ 其他                       │  │
│  └────────────────────────────┘  │
│                                 │
│  详细说明（选填）：              │
│  ┌─────────────────────────┐    │
│  │ 把负号弄丢了...         │    │
│  └─────────────────────────┘    │
│                                 │
│  [  取消  ]  [  确认标记  ]     │
└─────────────────────────────────┘
```

弹窗逻辑：

```javascript
function showErrorForm(questionId, stepNumber, chunkId, btnEl) {
    // 获取点击处的步骤难度分
    // 若 score <= 1，追加提示："该步难度较低（X分），建议优先关注高难度步骤"
    // 若已有错因记录，改为"修改错因"模式（预填已有数据）

    // 渲染浮层
    // mistake_type 用 <select>
    // 确认 → POST /questions/{qid}/step-error
    // 成功后按钮变绿 + "已标记"
}
```

### 5.3 取消标记

```javascript
function toggleErrorFlag(questionId, stepNumber, chunkId, btnEl) {
    var hasError = btnEl.dataset.hasError === 'true';
    if (hasError) {
        // 显示 "[删除标记]" 确认
        // DELETE /questions/{qid}/step-error/{eid}
        // 成功后恢复默认状态
    } else {
        showErrorForm(questionId, stepNumber, chunkId, btnEl);
    }
}
```

### 5.4 页面恢复加载错因

```javascript
function loadErrorFlags(questionId) {
    // 若 qid 存在，fetch('/questions/' + qid)
    // 读取 step_errors，更新每步的标记状态
}
```

---

## 六、前端改动 — history.html（个人题库）

### 6.1 列表视图增加"错题筛选"

在搜索框区域增加标签：

```html
<div class="history-tabs">
    <button class="history-tab active" data-filter="all" onclick="switchFilter('all')">全部</button>
    <button class="history-tab" data-filter="errors" onclick="switchFilter('errors')">
        ⚠️ 错题
        <span class="tab-count" id="errorCount">0</span>
    </button>
</div>
```

- 样式复用 `.type-btn`
- "错题"标签显示有错因的题目总数
- 点击调 `GET /questions/errors`，点击"全部"调 `GET /questions`

### 6.2 列表卡片增加错题标识

```css
.history-card.has-error {
    border-left: 3px solid #e53935;
}
```

渲染时若 `q.has_error`，为卡片添加 `has-error` class。

### 6.3 详情视图 — 步骤红标

在 `displayDetailResult()` 的步骤循环中：

```javascript
var errors = data.step_errors || [];

var stepErrors = errors.filter(function(e) {
    return e.chunk_id === cr.chunk_id && e.step_number === step.step_number;
});

if (stepErrors.length > 0) {
    html += '<div class="step step-has-error">';
    html += '<span class="error-dot" title="有错因标记"></span>';
} else {
    html += '<div class="step">';
}

// 步骤底部展示错因详情
if (stepErrors.length > 0) {
    html += '<div class="step-error-list">';
    for (var ei = 0; ei < stepErrors.length; ei++) {
        html += '<div class="step-error-item">';
        html += '<span class="error-type">' + escapeHtml(stepErrors[ei].mistake_type) + '</span>';
        if (stepErrors[ei].mistake_detail) {
            html += '<span class="error-detail">' + escapeHtml(stepErrors[ei].mistake_detail) + '</span>';
        }
        html += '<span class="error-time">' + stepErrors[ei].created_at + '</span>';
        html += '</div>';
    }
    html += '</div>';
}
```

### 6.4 样式

```css
.step-has-error { border-left: 3px solid #e53935; }
.error-dot {
    display: inline-block; width: 8px; height: 8px;
    background: #e53935; border-radius: 50%;
    margin-left: 6px; vertical-align: middle;
}
.step-error-list {
    margin-top: 8px; padding: 8px 12px;
    background: #fff5f5; border-radius: 8px; font-size: 13px;
}
.step-error-item { display: flex; gap: 8px; align-items: center; padding: 4px 0; }
.error-type {
    font-weight: 600; color: #c62828; background: #ffebee;
    padding: 2px 8px; border-radius: 4px; font-size: 12px; white-space: nowrap;
}
.error-detail { color: #515154; flex: 1; }
.error-time { color: #86868b; font-size: 12px; white-space: nowrap; }
```

### 6.5 详情页也支持直接标记

与 index.html 相同，每步底部保留"标记错因"按钮。

---

## 七、MistakeType 校验

前后端复用同一份枚举。前端 `<select>` 硬编码七个 option，后端 `_VALID_MISTAKE_TYPES` set 验证。后期扩展时两边同步更新。

---

## 八、API 变更总览

| 方法 | 路径 | 说明 | 变更 |
|------|------|------|------|
| POST | `/questions/{qid}/step-error` | 记录一条错因 | 新增 |
| DELETE | `/questions/{qid}/step-error/{eid}` | 删除一条错因 | 新增 |
| GET | `/questions/errors` | 有错因的题目列表 | 新增 |
| GET | `/questions/{qid}` | 增加 `step_errors` 字段 | 修改 |

修改错因 = 删除 + 重新标记，不做 update。

---

## 九、数据流总览

### 9.1 标记流

```
用户点击"标记错因" → overlay 弹出 → 选类型 + 输入说明 → 确认
→ POST /questions/{qid}/step-error → 写入 step_errors
→ 按钮变绿"已标记"
```

### 9.2 错题列表流

```
个人题库 → 点击"错题"标签 → GET /questions/errors → 渲染（红色左边框）
→ 点击卡片进入详情 → GET /questions/{qid}（含 step_errors）
→ 有错因的步骤显示红色左边框 + 错因详情
```

### 9.3 取消标记流

```
点击已标记按钮 → 确认删除 → DELETE /questions/{qid}/step-error/{eid}
→ 恢复默认状态
```

---

## 十、难度过滤规则（Phase 3 用，Phase 1 仅提示）

Phase 1 不做自动化跳过。标记弹窗内对 score <= 1 的步骤加提示：

> "该步难度较低（X分），建议优先关注高难度步骤"

Phase 3 中此规则用于交互控制：

| 难度分 | 交互方式 |
|--------|----------|
| 0 | 直接展示答案，自动确认 |
| 1 | 展示答案 + "懂了"确认按钮 |
| 2-3 | 完整交互（问思路 → 比对 → 选择题） |

---

## 十一、不做的事

| 事项 | 原因 |
|------|------|
| 修改 Verifier prompt 生成 distractors | Phase 3 做 |
| 修改 Solver / Formatter | 管线稳定 |
| 整卷 OCR / 网页搜索 API | 本版本只预留 source schema，API 等独立版本再做 |
| 自动化错因判定 | Phase 3 核心功能 |
| 错因统计/分析 | 数据足够后再做 |
| 侧边栏移动端适配 | 已延期 |

---

## 十二、开发顺序

| 步 | 内容 | 文件 | 估计 |
|----|------|------|------|
| 1 | 数据库建表（step_errors）+ questions 表加 source 列 + 五个查询函数 | `database.py` | 40 行 |
| 2 | 三个新 API + 一个修改 | `main.py` | 30 行 |
| 3 | index.html 标记按钮 + 浮层 JS | `index.html` | 60 行 |
| 4 | history.html 错题筛选 + 红标 + 标记 | `history.html` | 80 行 |
| 5 | style.css 错因样式 | `style.css` | 30 行 |
| 6 | 全链路测试 | 手动 | — |

---

## 十三、风险与边界

1. **旧题无 step_errors**：返回 `[]`，前端判断 `length > 0` 即可
2. **标记时 question_id 获取**：搜题未入库时无 qid，按钮置灰提示"请先保存到个人题库"
3. **多用户冲突**：SQLite 单文件，本版本不处理
4. **同题同步多次标记**：允许，前端全部列出，追踪反复错误
