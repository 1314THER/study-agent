# V0.3 第一阶段 — 技术方案

> 范围：前端三页（搜题做题 / 手把手教学·预留 / 历史题目）+ 侧边栏 + 入库控制 + 页面记忆
> 不动 prompts、不动 Verifier、不动 categories
> 后端改动 3 处，合计约 20 行

---

## 一、文件结构

```
frontend/
├── index.html      # 搜题做题页（原 index.html 改，+侧边栏 +保存开关 +localStorage）
├── teach.html      # 手把手教学·预留（新建，结构同 index，内容占位）
├── history.html    # 历史题目（新建，列表 + 详情双视图）
└── style.css       # 新建，提取全部公共样式

backend/            # 以下三处微改
├── main.py         # (1) SolveRequest 加 save 字段  (2) 新增 GET /questions/{id}
├── solver.py       # (3) solve_multi 加 save 参数判断
└── database.py     # (4) 新增 get_question_by_id()
```

页面间无共享 JS 运行时——不像 SPA 那样用 hash 路由切 div，而是三个独立 HTML，各含自己需要的 `<script>`。`style.css` 是唯一共享文件。

---

## 二、后端改动（4 处，约 25 行）

### 2.1 `main.py` — SolveRequest 加 `save` 字段

```python
class SolveRequest(BaseModel):
    question: str
    question_type: Optional[str] = Field(None)
    teacher: Optional[str] = Field(None)
    save: bool = True          # ← 新增，默认 True 保持向后兼容
```

同时给 `Step3Request` 也加同名字段（前端三阶调用最后一步发保存信号用）：

```python
class Step3Request(BaseModel):
    question: str
    chunk_results: str
    teacher: Optional[str] = Field(None)
    save: bool = False         # ← 新增，默认 False（solver.py 已有默认 True）
```

### 2.2 `main.py` — 新增 `GET /questions/{qid}`

```python
@app.get("/questions/{qid}")
def get_question(qid: int):
    """返回单题完整记录（含 answer_json）"""
    from backend.database import get_question_by_id
    result = get_question_by_id(qid)
    if result is None:
        return {"error": "not_found", "id": qid}
    return result
```

### 2.3 `solver.py` — `solve_multi()` 加 `save` 参数

```python
def solve_multi(question: str, question_type: str = None,
                teacher: str = None, save: bool = True) -> dict:
    # ... 前面逻辑不变 ...
    
    # 入库（加判断）
    if save:
        try:
            first = chunk_results[0] if chunk_results else {}
            final["category"] = first.get("category")
            final["difficulty"] = first.get("difficulty")
            save_question(question, final)
        except Exception as e:
            print(f"[Warn] 保存到数据库失败: {e}")
    
    return final
```

同时把 `/solve` 路由的调用也传过去：

```python
@app.post("/solve")
def api_solve(req: SolveRequest):
    return solve_multi(req.question, req.question_type,
                       teacher=req.teacher, save=req.save)
```

### 2.4 `database.py` — 新增 `get_question_by_id()`

```python
def get_question_by_id(qid: int) -> Optional[dict]:
    """返回完整记录（含 answer_json），所有 JSON 字段已解析"""
    conn = get_connection()
    row = conn.execute("SELECT * FROM questions WHERE id = ?", (qid,)).fetchone()
    conn.close()
    if not row:
        return None
    d = dict(row)
    for key in ("answer_json", "difficulty_dimensions",
                "knowledge_points", "common_mistakes"):
        val = d.get(key)
        if val:
            try:
                d[key] = json.loads(val)
            except (json.JSONDecodeError, TypeError):
                pass
    return d
```

---

## 三、前端全局结构

### 3.1 侧边栏（三页统一）

```html
<body class="theme-liangliang">
  <div class="app-layout">
    <aside class="sidebar">
      <a href="/" class="sidebar-logo">
        AI<br>学习伴侣
      </a>
      <nav>
        <a href="index.html" class="nav-item" data-page="solve">
          <span class="nav-icon">🔍</span>
          <span class="nav-label">搜题做题</span>
        </a>
        <a href="teach.html" class="nav-item" data-page="teach">
          <span class="nav-icon">📖</span>
          <span class="nav-label">手把手教学</span>
          <span class="nav-badge">预留</span>
        </a>
        <a href="history.html" class="nav-item" data-page="history">
          <span class="nav-icon">📋</span>
          <span class="nav-label">历史题目</span>
        </a>
      </nav>
    </aside>
    <main class="main-content">
      <div class="container"><!-- 各页内容 --></div>
    </main>
  </div>
</body>
```

**三个文件各自写死哪个 nav 高亮**，不靠 JS 判断——因为文件是静态的，当前页面是什么在写 HTML 时就知道了。

### 3.2 侧边栏样式（收在 style.css）

| 属性 | 值 |
|---|---|
| `.sidebar` | 固定定位，`left:0, top:0, width:72px, height:100vh`，`background:#1d1d1f`，flex 列居中 |
| `.nav-item` | flex 列居中，`width:64px, padding:12px 4px`，`border-radius:12px`，`color:#86868b` |
| `.nav-item:hover` | `background:rgba(255,255,255,0.1), color:#fff` |
| `.nav-item.active` | 主题色（跟随 teacher theme）：`color + background:rgba(theme,0.15)` |
| `.main-content` | `margin-left:72px`，`padding:30px 20px`，`min-height:100vh` |
| `.main-content .container` | `max-width:800px, margin:0 auto` |

侧边栏背景固定深色，不跟随老师主题切换。只有 active nav 的 accent color 跟随主题。

### 3.3 `style.css` 来源

直接从当前 `index.html` 的 `<style>` 块全量搬出，再追加侧边栏样式约 60 行。不删不减——当前的前端样式全部保留。

---

## 四、`index.html` — 搜题做题页

### 4.1 新增元素

**保存开关**（加在搜索框和进度区之间）：

```html
<div class="save-toggle">
  <label class="toggle-label">
    <input type="checkbox" id="saveToggle" checked>
    <span class="toggle-slider"></span>
    <span class="toggle-text">保存到小题库</span>
  </label>
</div>
```

这是一个拨动开关（toggle switch），不是裸 checkbox。需要追加约 30 行 CSS 在 style.css。

### 4.2 调用链改动

当前三步调用是 `step1 → step2 → step3`，只有 `/solve` 会触存入库。改动：

在 step3 的请求体加 `save` 字段：

```javascript
const r3 = await fetch('http://127.0.0.1:8000/solve/step3', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        question: question,
        chunk_results: JSON.stringify(d2.chunk_results),
        teacher: _selectedTeacher,
        save: document.getElementById('saveToggle').checked  // ←
    })
});
```

这样走三阶 API 也能控制入库。`save` 默认 `false`，勾选时才存。

### 4.3 localStorage 持久化

| key | 时机 | 用途 |
|---|---|---|
| `teacher` | 用户切换老师时写 | 跨页恢复 |
| `solve_input` | 每次输入变化时写（debounce 500ms） | 刷新恢复输入框 |
| `solve_save` | 开关变化时写 | 记住用户偏好 |
| `solve_result` | step3 成功后写 | 刷新后恢复上次结果 |

**恢复逻辑**（`DOMContentLoaded` 时执行）：

```javascript
// 1. 恢复老师选择
const savedTeacher = localStorage.getItem('teacher') || 'liangliang';
selectTeacher(document.querySelector(`[data-teacher="${savedTeacher}"]`), savedTeacher);

// 2. 恢复输入框
const savedInput = localStorage.getItem('solve_input');
if (savedInput) {
    document.getElementById('questionInput').value = savedInput;
    autoResize(document.getElementById('questionInput'));
}

// 3. 恢复保存开关
const savedSave = localStorage.getItem('solve_save');
if (savedSave !== null) {
    document.getElementById('saveToggle').checked = savedSave === 'true';
}

// 4. 恢复上次结果
const savedResult = localStorage.getItem('solve_result');
if (savedResult) {
    try {
        const { data, totalTokens } = JSON.parse(savedResult);
        displayResult(data, totalTokens);
    } catch(e) {
        localStorage.removeItem('solve_result');
    }
}
```

**什么时候清除 `solve_result`**：用户点了搜索按钮时清除。

### 4.4 其余不变

老师选择、进度条、定时器、三步 API 调用、`displayResult()`、`displayXuebile()`、`renderSteps()`、`toggleDetail()`、雷达图画图——全都不动。

---

## 五、`teach.html` — 手把手教学·预留

### 5.1 布局

与 `index.html` 共享相同的：
- 侧边栏（`teach` 高亮）
- 老师选择器（保留，可正常切换）
- `style.css`

### 5.2 内容区

```html
<h1>手把手教学</h1>
<p class="subtitle">功能开发中，敬请期待</p>

<!-- 老师选择器：同 index.html，保留 -->

<!-- 占位区，替代搜索框 + 进度 + 结果 -->
<div class="placeholder-card">
    <div class="placeholder-icon">📖</div>
    <h2>即将上线</h2>
    <p>将支持分步骤互动教学，每一步让你选择答案，AI 即时判断对错并给出错因分析</p>
    <div class="placeholder-features">
        <div class="pf-item">✓ 选择题模式，点击即反馈</div>
        <div class="pf-item">✓ "我不是这么做的" 自由输入</div>
        <div class="pf-item">✓ 错因分类与沉淀</div>
    </div>
</div>
```

### 5.3 样式（追加到 style.css）

```css
.placeholder-card {
    background: white;
    border-radius: 16px;
    padding: 60px 40px;
    text-align: center;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
}
.placeholder-icon { font-size: 64px; margin-bottom: 16px; }
.placeholder-card h2 { font-size: 24px; color: #1d1d1f; margin-bottom: 12px; }
.placeholder-card p { font-size: 15px; color: #86868b; line-height: 1.6; margin-bottom: 24px; }
.placeholder-features { display: inline-block; text-align: left; }
.pf-item { font-size: 14px; color: #515154; padding: 6px 0; }
```

### 5.4 JS

只保留 `selectTeacher()` 和相关切换逻辑 + localStorage 读写 `teacher`。不需要三步调用、progress、结果渲染、雷达图。

### 5.5 localStorage

页面加载时从 `localStorage.getItem('teacher')` 恢复老师选择。

---

## 六、`history.html` — 历史题目页

### 6.1 双视图结构

```html
<h1>历史题目</h1>
<p class="subtitle" id="historyCount">加载中...</p>

<!-- 列表视图 -->
<div id="listView">
    <!-- JS 渲染卡片 -->
</div>

<!-- 详情视图（默认隐藏） -->
<div id="detailView" style="display:none">
    <button class="back-btn" onclick="backToList()">← 返回列表</button>
    <div id="detailResult"></div>
</div>
```

### 6.2 列表视图

**数据来源**：`fetch('/questions')`

**卡片渲染**（每题一张）：

```html
<div class="history-card" data-id="{id}" onclick="showDetail({id})">
    <div class="hc-preview">{content 截取前 60 字...</div>
    <div class="hc-meta">
        <span class="tag tag-category">{category_level1}</span>
        <span class="tag {difficultyClass}">{difficulty_level} ({difficulty_score}分)</span>
        <span class="hc-date">{created_at}</span>
    </div>
</div>
```

**样式**：

```css
.history-card {
    background: white; border: 1px solid #e0e0e0; border-radius: 12px;
    padding: 16px 20px; margin-bottom: 12px; cursor: pointer;
    transition: all 0.15s;
}
.history-card:hover { border-color: #0071e3; box-shadow: 0 2px 8px rgba(0,113,227,0.1); }
.hc-preview { font-size: 15px; color: #1d1d1f; line-height: 1.5; margin-bottom: 10px; }
.hc-meta { display: flex; gap: 8px; align-items: center; flex-wrap: wrap; }
.hc-date { font-size: 13px; color: #86868b; margin-left: auto; }
```

**空态**：

当列表为空时显示：

```html
<div class="empty-state">
    <div class="empty-icon">📋</div>
    <p>还没有搜过题</p>
    <p class="empty-desc">去搜题做题页搜索题目，结果会自动保存在这里</p>
    <a href="index.html" class="empty-link">去搜题 →</a>
</div>
```

**加载态**：显示骨架屏三张卡片（灰色渐变脉冲动画）。

### 6.3 详情视图

**数据来源**：`fetch('/questions/' + id)`

**渲染**：拿到 `answer_json`，直接传给 `displayResult(data, null)`。

这里 `displayResult()` 函数需要从 `index.html` 复制过来，包括：
- `displayResult()`
- `renderSteps()`
- `escapeHtml()`
- `toggleDetail()`
- 雷达图画图代码
- `levelClass()`

**差异点**：
- 不显示 token 消耗行
- 不显示进度条（已经在列表页了）
- 顶部有一个 "← 返回列表" 按钮

**返回按钮**：

```javascript
function backToList() {
    document.getElementById('detailView').style.display = 'none';
    document.getElementById('listView').style.display = 'block';
    document.getElementById('historyCount').style.display = 'block';
}
```

### 6.4 搜索/筛选（预留）

列表顶部预留一个搜索框，但先 disabled：

```html
<div class="history-search">
    <input type="text" placeholder="搜索题目...（即将上线）" disabled>
</div>
```

不增加后端逻辑，仅占位。

### 6.5 localStorage + 缓存

```javascript
// 加载时，先读缓存展示
const cached = localStorage.getItem('questions_cache');
if (cached) {
    try { renderList(JSON.parse(cached)); } catch(e) {}
}
// 再请求最新数据
fetch('/questions').then(r => r.json()).then(data => {
    renderList(data);
    localStorage.setItem('questions_cache', JSON.stringify(data));
});
```

---

## 七、`style.css` 内容大纲（约 500 行）

文件来源：从当前 `index.html` 的 `<style>` 块直接移出，追加以下部分：

### 追加内容

| 区块 | 约行数 | 说明 |
|---|---|---|
| `.app-layout` / `.sidebar` / `.main-content` | 30 | 左右分栏布局 |
| `.nav-item` 样式 + 各主题 active 色 | 40 | 侧边栏导航 |
| `.save-toggle` / `.toggle-*` | 30 | 保存开关拨动样式 |
| `.history-card` / `.hc-*` | 30 | 历史列表卡片 |
| `.placeholder-card` / `.placeholder-*` | 20 | teach 页占位卡 |
| `.empty-state` / `.empty-*` | 20 | 空态展示 |
| `.back-btn` | 10 | 返回按钮 |
| `.history-search` | 10 | 搜索框（预留） |

总计追加约 190 行，合并原样式约 380 行，`style.css` 约 570 行。

---

## 八、数据流总览

### 8.1 搜题做题流

```
用户输入 → 点搜索
  → 读 saveToggle.checked
  → step1: /solve/step1 (不变)
  → step2: /solve/step2 (不变)
  → step3: /solve/step3 { save: checked状态 }  ← 唯一变化
     → 后端：if save: save_question(question, final)
  → displayResult()
  → 写 localStorage (input, result, teacher, save)
```

### 8.2 历史列表流

```
页面加载
  → 读 localStorage.questions_cache → 立即渲染（防闪白）
  → fetch /questions → 重新渲染 → 更新缓存
  → 用户点击某题卡片
     → 隐藏列表，显示详情
     → fetch /questions/{id}
     → answer_json 喂给 displayResult()
  → 用户点返回 → 隐藏详情，显示列表
```

### 8.3 跨页状态流

```
用户进入 index.html
  → onload: localStorage.teacher → selectTeacher()
  → 切换到 teach.html
  → onload: localStorage.teacher → selectTeacher()
  → 侧边栏颜色跟随老师主题
```

---

## 九、关键设计决策记录

| 决策 | 选择 | 理由 |
|---|---|---|
| 拆三个 HTML 还是一个 SPA | 三个独立 HTML | 页面间无共享运行时状态，拆开更清晰，主文件不膨胀 |
| CSS 共享方式 | 一个 `style.css` | 后续多页功能继续加，公共样式统一管理 |
| JS 是否共享 | 不共享 | 各页功能差异大，`displayResult()` 复制到 history.html 即可，抽公共 JS 不划算 |
| 入库触发时机 | step3 回传时带 `save` 参数 | 复用现有三阶流程，不改前端三步串行逻辑 |
| 旧 `/solve` 接口兼容性 | `save` 默认 `true` | 保持现有调用方（如 curl 测试）行为不变 |
| 跨页老师选择恢复 | localStorage | 页面间无运行时通信，localStorage 是唯一可行的跨页共享方式 |
| 历史题目详情 | 新接口 `GET /questions/{id}` | `GET /questions` 不含 `answer_json`，加新接口更干净 |
| teach 预览保留老师选择器 | 保留 | 后续开发生成干扰项后直接可用，不需要再回头加 |
| 骨架屏 vs loading 文字 | 骨架屏（三张灰色卡片） | 列表加载需要，视觉舒适 |

---

## 十、未覆盖的边界与待确认

1. **侧边栏移动端适配**：当前只考虑桌面。移动端 72px 侧边栏占用太大。方案：屏幕 < 768px 时侧边栏折叠为底部 tab bar，或隐藏为汉堡菜单。**本次不做，留到 V0.3 第二阶段。**

2. **多点登录 / 多用户**：SQLite 是单文件，没有用户概念。`save` 开关只控制是否落库，不涉及用户归属。**本次不做，远期再说。**

3. **重复历史记录的 UI 去重**：同一个用户搜索同一题两次，数据库 `UNIQUE(content)` 只存一条，列表不会重复显示。无需额外处理。

4. **`solve_result` 存满 localStorage**：单条结果 JSON 大约 5-15KB，localStorage 配额 5MB，完全够用。但最多只存最近一条，不会累积。

5. **`questions_cache` 过期策略**：暂无过期机制，只会在列表加载后自动覆盖。用户可以刷新页面拿最新数据。
