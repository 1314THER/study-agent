# 数学AI学情系统 · 开发路线图

> 项目定位：一个学生自驱、AI诊断、教师监控的数学学习闭环。
> 核心信条：**AI 做的是人类无法做到的事情**——为每个学生的每道题的每一小步标出知识点、难度、错因。

---

## 当前状态（v0.4）

- 三阶管线（Solver → Verifier → Formatter）已跑通
- 多老师路由（liangliang/taotao/xuefeng/ji）
- 题库 + 组卷PDF导出 + 筛选分页 + 错因标记
- 多模态文件解析（PDF/docx/txt/图片）
- 准确率：~99%（实测100+题）

## 当前可识别的短板

- 9个 verifier prompt 大量重复，未模板化
- 知识点字典不够细（导数只有"导数综合"一个子类，缺少复数/平面向量/不等式等独立板块）
- 正则解析无 fallback，LLM 输出格式轻微变化即崩
- 前端全 vanilla JS，history.html 单文件1100+行
- 数据库迁移代码有多余的 try-except

---

## Phase 0 — 地基重构（2天，度假前完成）

### 0.1 Prompt 模板化

**目标**：消除 verifier prompt 的85%重复

- 创建 `backend/prompts/verifier_template.md`，用 `{category}` 和 `{knowledge_points}` 占位
- `solver.py` 读取模板时注入实际值
- 保留原9个 markdown 文件作为 git history，但不再手动维护
- extraction.md 太长（588行），拆成核心prompt + 示例附录

**验收标准**：跑之前同样的10道题，输出格式与重构前完全一致。

### 0.2 知识点字典（单数据源）

**目标**：知识点定义只写一次，prompt 和代码都从它生成

- 新建 `backend/categories.yaml`（YAML，比 Python dict 更易直接喂给 prompt 生成）
- 板块从7个扩展到14个，补上：**复数、平面向量、不等式、集合与逻辑用语、算法初步、坐标系与参数方程、计数原理**
- 导数拆成子类：极值点偏移、隐零点、端点效应、双变量问题、恒成立与存在性问题、切线综合
- `categories.py` 改为读 YAML
- prompt 生成脚本也读 YAML

```
backend/
  categories.yaml    ← 唯一数据源
  categories.py      ← 读 YAML 提供 Python API
  prompts/
    verifier_template.md      ← 用 {category}/{knowledge_points}
    verifiers/                ← 不再手动维护，仅存历史
    solver.md                 ← 板块列表改为从 YAML 动态生成
```

### 0.3 管线稳定性加固

**目标**：正则解析不崩管线

- 解析函数加三层逻辑：精确正则 → 宽松行匹配 → 原始文本兜底
- 所有 LLM 原始输出完整存入数据库（`questions.raw_solver_output` / `raw_verifier_output` 新列）
  - 需要先做一次数据库迁移
- `_extract_json` 重写为 brace-depth tracking
- `main.py` 的所有 inline import 提到文件顶部

---

## Phase 1 — 学生端：手把手教学 + AI 生题（1周）

### 1.1 交互式做题模式（手把手教学）

**核心逻辑**：学生一步一步写，AI 一步一步看，不是 AI 直接给答案。

**前端交互**：

```
┌─────────────────────────┐
│ 题目显示区（含 LaTeX）     │
│                         │
│ ┌───────────────────┐   │
│ │ 第1步输入框          │   │ ← 学生写
│ │ [写什么]            │   │
│ └───────────────────┘   │
│ 反馈区：AI 看刚写的步骤      │ ← AI 实时审
│ "方向对了，但你忘了考虑... "  │
│                         │
│ ┌───────────────────┐   │
│ │ 第2步输入框          │   │
│ │ [写什么]            │   │
│ └───────────────────┘   │
│ ...                     │
└─────────────────────────┘
```

**关键约束**：
- AI 不能直接写出下一步答案——只能评价、提示方向、指出遗漏条件
- 每步都要在后台跑一次 verifier（但温度设低，只做"这步对不对"的判断，不做完整解析）
- 学生卡住超过2分钟，AI 主动给一个提示（渐进式：小提示 → 中等提示 → 几乎给答案）

**后端新增接口**：

| 接口 | 说明 |
|---|---|
| `POST /teach/check-step` | 传题目 + 当前步骤序号 + 学生写的内容 → 返回方向评价 / 错误提醒 / 提示 |
| `POST /teach/hint` | 传题目 + 步骤序号 → 返回渐进式提示（level 1/2/3） |
| `POST /teach/finalize` | 学生完成所有步骤后 → 整体评价 + 知识点标定 + 错因 |

**prompt 设计要点**：
- forbid 给答案——必须用"提示性语言"描述思路
- 能识别"学生虽然过程不同但思路等价"
- 能识别"学生写了错误路径"并指出来

### 1.2 AI 自定义生题

**核心逻辑**：给定约束 → 生成一道自洽的题。

**输入**：
- 知识点（如"极值点偏移"）
- 难度（容易/中等/困难）
- 题型（选择题/填空题/大题）
- 可选：题目背景、关键词约束

**输出**：
- 题目文本（LaTeX）
- 参考答案
- 难度自评（跟 Solver 跑一遍确认）
- 知识点标签

**关键质量控制**（由AI队友负责）：
- 答案必须唯一——不能出现"参数a=2或a=-2"这种歧义
- 条件必须自洽——不能"题目给了两个条件互相矛盾"
- 难度必须在可预测范围内——生出来的题用 Solver 跑一遍，跟设定的难度标签对比
- 不能出现超纲内容

**前端交互**：
```
知识点：[下拉选择]  难度：[选择]  题型：[选择]
[关键词（可选）]
[生成] → 预览题目 + [再用 Solver 跑一遍确认]
```

**生题验证管线**（重要）：
```
生题 prompt → 输出题目 → Solver 跑一遍 → 对比 Solver 输出的难度和标签是否与设定一致
                                    ↓ 不一致
                                 自动调整参数重新生成（最多3次）
                                    ↓ 仍不一致
                                 标记为"低置信度"，人工审核
```

---

## Phase 1.5 — 单题 OCR 过程诊断（2-3天）

### 核心逻辑

学生手写解题过程（拍一张照）→ OCR 识别手写文字 + 公式 → 对比该题的 Solver 标准过程 → 标出学生的哪一步有错、哪一步跳了、哪一步跟标准路径不同但有道理的等价路径。

**注意**：这个功能的核心不是 OCR 本身（现有 API 够用），而是 **"把手写内容跟结构化标准过程做对齐"**——这是最难的部分。

**实现方案**：
1. 用通义 Qwen-VL 或百度 OCR 把图片转成文本（含部分 LaTeX）
2. 用一段 prompt 把 free text 做"步骤对齐"：识别学生写了N步，每步对应标准过程的哪一步
3. 对比每步，输出错因
4. 如果 OCR 结果太差（latex 乱码占30%以上），退回给用户"请重新拍照"

**prompt 设计**（这个要单独写一个）：
```
你是一个数学批改老师。你有两道过程：标准过程（分步）和学生手写过程（OCR识别结果）。
请做：
1. 识别学生写了几步，每步对应标准过程的第几步
2. 如果学生的写法跟标准过程不同但等价，标注为"正确，等价路径"
3. 如果学生的某步有错误，标出错误类型和位置
4. 如果学生跳过了关键步骤，标出"省略了关键推导"
```

---

## Phase 2 — 试卷维度（1周）

### 2.1 整卷批改 + 评价（接口重构）

已有 `/solve/step3` 可以逐个批改。需要新做一个 `/exam/grade`：

- 输入：题目列表（有序） + 学生答案列表
- 输出：每题的诊断 + 整卷知识点覆盖分析 + 薄弱区域

### 2.2 自动出整卷

- 输入：知识点列表 + 各题型数量 + 总难度
- 输出：一张完整试卷（题目 + 分值分布 + 参考答案 + 各题知识点标定）
- 质量要求：不能出现同一道题涉及的知识点重复率过高，各题之间条件不能互用

### 2.3 扫描答题卡改卷

**最难的功能之一。**

当前简化方案：
1. 整张答题卡图片 → OCR 分割试题区域
2. 每题独立送 Solver 管线对比标准答案
3. 汇总结果 + 每题的错因

**实现的坑**：
- 答题卡版面检测（哪道题的答案写在哪）需要版面分析能力
- 手写答案跟标准答案的语义匹配——学生写"x=2"标准答案是"x=2或x=-2"，这是半对
- 选择题直接用选项匹配即可，相对简单

**建议**：第一版只支持 **选择题+填空题** 的答题卡批改，大题走手把手教学模式分步批。大题答题卡识别单独做一版。

---

## Phase 2.5 — 实时时间戳输入（2-3天）

**纯前端工作**。不需要后端改动。

- 每个输入框/步骤记录：`step_start_time, keypress_events[], step_end_time`
- 每步提交时，把时间数据序列化附在请求里
- 后端在错因记录中多存一个 `time_series: JSON` 字段

**这个数据的价值**：一段时间之后可以回答这些问题——
- 这类知识点学生平均卡多久？
- 某类错因跟做题速度有相关性吗？
- 学生在一天中什么时候做数学最清醒？

---

## Phase 3 — 个人学情规划（几天）

### 3.1 个人题库提炼

- 所有做过的题按知识点聚簇
- 每个知识点显示：做题数、正确率、典型错因TOP3、平均耗时
- 视图：列表视图 / 雷达图 / 趋势折线图

### 3.2 薄弱点聚类

**DS 同学的主场。**

- 把错因按知识点+题型+难度三维聚成"薄弱模式"
- 例：A同学在"导数综合·恒成立问题"的"分类讨论"步骤上连续错5次——这不是粗心，是结构性漏洞
- 输出一个"待攻克知识点"排序列表

### 3.3 提升趋势

- 按周/月粒度，每个知识点的正确率变化曲线
- 能力雷达图的时间对比（第1周 vs 第4周）
- 如果某个知识点连续4周没有提升，自动标记为"瓶颈"

### 3.4 未来路径推荐

- "当前你需要重点攻克A和B两个知识点，建议练习顺序是：先B再A"
- 推荐依据：知识点前置依赖关系 + 当前掌握度 + 高考考频
- 每条推荐附带"选择理由"——让用户不只是得到一个推荐，而是理解为什么

---

## Phase 4 — 教师端（暑假剩余，2-3周）

### 4.1 全班统计面板

```
┌─────────────────────────────────────────┐
│ 班级：高三(1)班    51人     最近活跃：49/51 │
├─────────────┬───────────────────────────┤
│ 知识点热力图  │ 异常检测列表                │
│             │ • 张三  24h未做题           │
│  解析几何 ██░│ • 李四  导数连续错3次        │
│  立体几何 ███│ • 王五  做题量骤降↓70%      │
│  函数导数 ░░░│                           │
│  数列    ██░ │                           │
│  概率统计 ███│                           │
└─────────────┴───────────────────────────┘
```

### 4.2 个性化作业分发

输入示例：
```
指令：给以下学生各4道大题
  - A：解析几何（压轴题难度）
  - B：导数综合（中等难度）
  - C：数列 + 概率各2道
截止：明天晚上8点
```

系统处理：
1. 从题库/生题模块找出匹配的题
2. 每道题附上"选择理由"
3. 一键通知学生端
4. 学生完成后，结果自动回传给教师端

### 4.3 实时状态监控

- 当前在线学生数
- 当前正在做哪道题、卡在第几步
- 如果某个学生在某步卡了超过5分钟，老师端会有提示
- 老师可以点击"发送消息"给学生一个鼓励/提醒

### 4.4 自动生成习题课内容

输入：全班在某知识点上的错因分布
输出：一份教案草稿（PPT大纲级）

- 这个知识点的常见错误类型列出来
- 每种错误配一道错题（来自学生数据，脱敏）
- 配一道正确示范题
- 课堂练习建议

**不要求AI直接生成完整PPT，只生成Markdown大纲，老师可以自己调整。**

---

## Extra — Agent 智能助手入口（简单，2天）

- 侧边栏一个对话框或语音入口
- 自然语言 → 路由到后台对应功能
- 示例指令集：

| 指令 | 路由到 |
|---|---|
| "帮我出3道导数压轴题" | 生题模块 |
| "给A同学发一条提醒消息" | 教师端消息系统 |
| "我上周的薄弱点是什么" | 学情规划模块 |
| "帮我批改这张答题卡" | 答题卡识别模块 |

实现方式：
- 一个简单的 prompt 做意图分类（加一道路由分类器）
- 参数提取（用 few-shot prompt 或者 function calling）
- 调对应 API

---

## 汇总时间线

```
Week 1     Phase 0 地基重构
Week 2     度假
Week 3-4   Phase 1 手把手教学 + AI 生题
Week 5     Phase 1.5 单题 OCR 过程诊断
Week 6-7   Phase 2 试卷维度
Week 7     Phase 2.5 实时时间戳
Week 8     Phase 3 个人学情规划
Week 9-11  Phase 4 教师端
Week 12    Extra Agent 入口 + 打磨发布

预计总工时：10-12周（含度假1周）
三个人并行可压缩至 6-8 周
```

---

## 技术架构备忘（即将跨入的新模块依赖）

```
frontend/                        backend/
  index.html                       main.py ← API 路由 (膨胀后拆成多个 router)
  teach.html ← 重写交互式做题      teach.py ← Phase 1 手把手
  exam.html                        exam.py ← Phase 2 整卷
  history.html                     generator.py ← Phase 1.2 生题
  dashboard.html ← Phase 3 学情   ocr.py ← Phase 1.5/2.3 OCR
  teacher.html ← Phase 4 教师端   planning.py ← Phase 3 学情规划
  agent.html ← Extra 智能助手     teacher.py ← Phase 4 教师端
  style.css ← 膨胀到需要拆          agent.py ← Extra
```

---

---

## Phase 5 — 学校端：教学大纲智能反馈

### 5.1 数据驱动的教学调整

当你的系统在巴蜀云校这样的学校里跑满一个学期之后，你手上会有一份独特的数据资产：**全校学生在每个高考知识点上的掌握度分布、典型错因模式、能力变化趋势。**

这份数据不仅可以给学生用，也可以给学校用。

### 5.2 学校端输出报告

一个学期结束后，系统自动生成一份面向学校管理层的诊断报告：

```
高三数学组 · 上学期学情诊断报告
────────────────────────────────────
整体概览
  - 全年级知识点掌握度分布热力图
  - 各班级横向对比（匿名）
  - 本学期薄弱知识点TOP5

典型教学盲区
  - 全年级超过60%的学生在"导数综合·极值点偏移"上第3步（构造函数）出错
  - 这不是个别学生的问题，是这个知识点需要调整教学顺序

教学建议
  - 建议在讲极值点偏移之前，先巩固"导数运算"和"单调性判断"两个前置知识点
  - 建议增加"隐零点"的课堂练习量，目前练题量可能是正常量的1/3

教学大纲调整参考
  - 基于本年级实际数据，建议下学期的课时分配从 3:3:2:2 调整为 2:4:2:2
```

### 5.3 这个层次的价值

卖到B端学校的不是"解题工具"，而是**教学决策支持系统**。老师用它备课，教研组用它调大纲，学校用它做教学评估。

与巴蜀谈判时，你的叙事就不只是"我的AI能解题"，而是：

> 你们的学校每一届都在教同样的东西、用同样的课时分配、靠经验调整教学节奏。我提供的系统，可以给你们一个**数据反馈闭环**——这一届学生真实卡在哪里、哪里教学节奏可以优化、下一届怎么调整。你们成为在中国第一个用AI学情数据指导高考数学教学的学校。这不仅是买一个工具，这是一个教育改革的实验。

这个叙事对于巴蜀这样的重点中学是很有吸引力的——他们不缺教育资源，但他们缺"能用数据证明自己的教学决策是对的"的支撑。一个省级示范中学，如果能在对外汇报时说"我们基于AI学情系统的数据分析，调整了数学教学大纲，实验班成绩提升了XX%"，这是一个有分量的宣传点。

### 5.4 你的产品线完整了

```
学生端（个人学习）
  ├ 单题诊断
  ├ 手把手教学
  ├ AI生题练习
  ├ 整卷批改
  └ 个人学情规划

教师端（班级管理）
  ├ 全班数据看板
  ├ 个性化作业分发
  ├ 实时状态监控
  └ 习题课自动生成

学校端（教学管理）
  ├ 年级学情报告
  ├ 教学盲区识别
  ├ 大纲调整建议
  └ 跨班级/跨年级对比
```

前两层是你路线图里已有的。第三层是到巴蜀之后自然长出来的——也是你这个系统跟市面上所有教育产品最大的区别所在：**不是帮学生查漏补缺的，是帮学校发现教学盲区的。**

这个叙事会让你的谈判对象从信息中心主任，升到分管教学的副校长。
