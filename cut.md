# 待修复 / 待调整项

## 1. Qwen-VL 切题型不准

**问题**：发 4 道选择题过去，被切成了填空题。

**原因**：切题 prompt 里没有让模型输出题型（选择题/填空题/大题），模型自己猜的类型自然不准。已在 `multimodal.py` 的 `_EXTRACTION_PROMPT` 和 `_post_process` 中加上 `type` 字段输出，但仍需验证模型是否准确识别。

**需手动验证**：
- 上传几道选择题 → 看返回的 `question_type` 是否 = "选择题"
- 如果不准 → 调 prompt 措辞或换模型

---

## 2. 进度条体验不一致

**已在 multimodal.html 加了**：
- `_sessionTimers` 计时器（显示已思考 s / 180s，超时后显示"你做不过我你信吗"）
- `renderProgress` 显示教师名 + 百分比 + token 消耗
- 三段式进度：13%（思考）→ 58%（分类）→ 78%（检查）

**需确认**：进度条颜色是否跟随主题（和 index.html 一致？当前用默认蓝色渐变）

---

## 3. 入库规则（multimodal.html 已改）

**已改的**：
- `step3` 不再自动保存（`save: false`）
- 解题结果下方显示「📋 加入题库」按钮
- 左栏底部「📋 一键入库」按钮，把全部已解未入库的题批量存入
- 添加错因时自动先保存再添加

**需确认**：
- 如果用户没点「加入题库」也没加错因，题目不会被保存到个人题库——这是期望行为
- "左侧价格按钮" 可能打错字了，我理解的是"左栏加个按钮"

---

## 4. index.html（单题解答）入库规则

**现状**：还没改。

**需要改的**：
1. 去掉自动保存 toggle（`saveToggle` checkbox）
2. `solveQuestion()` 中调 step3 时 `save: false`
3. 解题完成后底部显示「📋 加入题库」按钮
4. 添加错因时自动先保存再获取 `qid`
5. 删除 `saveAfterSolve()` 和 `saveToggle` 相关代码

---

## 5. 组卷导航（exam.html）

**现状**：左侧导航已经加了第 5 项"📎 多模态解答"，缩进和格式都对了。

**如果你那边还是看不到**，可能是 sidebar 高度不够（第 5 个 nav-item 被裁了）。可以检查 CSS `.sidebar` 有没有 `overflow: hidden`。或者截个图我看看。

---

## 6. Qwen-VL 切题 prompt 的细节

**已在 `multimodal.py` 中改的**：
- prompt 增加了 `3. 题型判断（选择题 / 填空题 / 大题）`
- JSON 输出格式增加 `"type": "选择题"`
- `_post_process` 中提取 `q.get("type", ...)` 并返回 `question_type`

**需验证**：
- 用 Python 直接调一下看返回的 `question_type` 对不对
- 如果 model 乱给类型，考虑在 prompt 里加例子：
```
{"index": 1, "latex": "若集合 $A=\\{x\\mid x^2<4\\}$，则 $A$ 中的元素个数为\\nA. 1\\nB. 2\\nC. 3\\nD. 4", "type": "选择题", "difficulty": "简单"}
```

---

## 7. 文本输入也需要转 LaTeX

**现状**：多模态页面新增了文本输入框，用户可以直接贴题目文字。但是普通文本（如 "1. 解方程 2x+5=13"）直接发给 Qwen-VL，会被原样返回，不会自动转成 LaTeX 格式。

**需要修的**：
- 文本输入时，prompt 里加上"将题目文字转换为 LaTeX 格式"的要求
- 或者统一走切题流程，让 Qwen-VL 既切题又把文本转 LaTeX
- 即使用户输入的是纯文本，最终 `json.latex` 字段里也应该是带 `$...$` 或 `$$...$$` 的 LaTeX 格式

**当前效果**：现在文本输入也能切题，但返回的 `latex` 字段可能是纯文本（如 `"已知函数 f(x) = x^2 + 2x + 1，求 f(x) 的最小值。"`），而不是 LaTeX（如 `"已知函数 $f(x) = x^2 + 2x + 1$，求 $f(x)$ 的最小值。"`）。

**需验证**：切题 prompt 已经要求"用 LaTeX 格式"输出，理论上文本输入也会被转成 LaTeX。如果实际效果不对，需要调 prompt。
