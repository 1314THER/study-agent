# 管线架构总览

> 四管线 · 四步骤 · 知识点共享

---

## 一、总览

```
                    ┌──────────────────────────────────────────────────────────┐
                    │                  题目入库                                 │
                    │   PDF / Word / 手写图 / TXT                              │
                    └────────────┬─────────────────────────────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   Step 1: 多模态→LaTeX   │  ← 视觉模型
                    │   输出：纯文本 LaTeX          │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   Step 2: 切题            │  ← 轻量模型 + 规则
                    │   输出：题型+难度+知识点     │
                    └────────────┬────────────┘
                                 │
                 ┌───────────────┼───────────────────┐
                 │               │                    │
          ┌──────▼──────┐  ┌─────▼──────┐  ┌────────▼────────┐
          │  选择题管线  │  │ 填空题管线  │  │  解答题管线      │
          │  Step 3:     │  │ Step 3:    │  │  Step 3: 解题    │
          │  选项匹配    │  │  计算求解   │  │  (按板块 step 表) │
          │  Step 4:     │  │ Step 4:    │  │  Step 4: 评分    │
          │  结果确认    │  │  等价答案判 │  │  (逐步骤验证)    │
          │              │  │  断        │  │                  │
          └──────┬───────┘  └─────┬──────┘  └────────┬────────┘
                 │                │                   │
                 └──────────┬─────┴───────────────────┘
                            │
                     ┌──────▼──────┐
                     │   非常规题   │
                     │  Step 3:    │
                     │  AI 自由解题 │
                     │  Step 4:    │
                     │  AI 自判    │
                     └──────┬──────┘
                            │
                     ┌──────▼──────┐
                     │   入库打标签   │
                     │   知识点共享   │
                     │   老师可按     │
                     │   知识点搜题    │
                     └─────────────┘
```

---

## 二、四步骤详述

### Step 1: 多模态 → LaTeX

**职能：** 不管输入是什么格式，全部转成纯文本 LaTeX

**输入：** PDF / Word / 手写图片 / 纯文本 / 截图

**输出：** 一段干净的 LaTeX 字符串（不带排版、不带夹页、不带选项干扰）

**说明：**
- 这一步只做"视觉内容→文本"的转换，不做任何判断
- 手写题、印刷体 PDF、Word 文档全部走同一个入口
- 模型选侧重多模态理解能力的（如通义千问 VL、GPT-4o 视觉）
- 输出为纯文本后，后续步骤不再需要视觉能力

---

### Step 2: 切题

**职能：** 判断题型、难度、知识点归属

**输入：** Step 1 输出的 LaTeX 文本

**输出：**
```json
{
  "type": "选择题 | 填空题 | 解答题 | 非常规题",
  "difficulty": "容易 | 中等 | 困难",
  "category": "解析几何",
  "knowledge_points": ["椭圆基础", "抛物线基础"],
  "is_irregular": true | false,
  "reason": "自动判断的依据说明"
}
```

**说明：**
- 使用轻量模型（DeepSeek-V3 / GPT-4o-mini 级别即可），不需要多模态能力
- 辅以正则规则兜底：检测 A./B./C./D. 标记、"选择题"关键词、试卷题号位置等
- 输出后**允许人工编辑**——老师在后台看到推荐结果，可手动调整题型和知识点
- 人工编辑后，后续步骤按修改后的结果走

---

### Step 3: 解题

**职能：** 将题目解出，输出完整解答

**输入：** LaTeX 题目 + Step 2 的切题结果

**按题型分流：**

| 管线 | 行为 |
|---|---|
| **选择题** | 快速解题，输出答案 + 所考知识点。不列完整步骤，不关心学生怎么做的。 |
| **填空题** | 准确计算，输出答案 + 知识点。注意等价答案判断。 |
| **解答题** | 按板块的固定步骤表分步解题。每一行输出对应步骤表中的一步。 |
| **非常规题** | 无固定步骤表。AI 自己定步骤、自己切分、自己输出。 |

**解答题固定步骤表示例（解析几何）：**
```
椭圆基础 → 设点设线 → 联立 → 韦达定理 → 条件转化 → 代入计算 → 最值处理
```

AI 按这个表走，输出时每步带步骤名和计算结果。

---

### Step 4: 评分

**职能：** 对 Step 3 的解答做逐步骤校验，输出评分结果

**输入：** Step 3 的解答 + 原题

**输出：**
```json
{
  "status": "正确 | 部分正确 | 错误",
  "steps": [
    {"step_name": "联立", "status": "正确", "score": 3, "comment": ""},
    {"step_name": "韦达定理", "status": "部分正确", "score": 2, "comment": "x1+x2正确，x1x2符号错了"}
  ],
  "total_score": 10,
  "errors": ["第三步分类讨论漏了一种情况"],
  "knowledge_points": ["椭圆基础", "设点设线"]
}
```

**关键变更：**
- **不再调 LLM 做 formatter。** 所有 LaTeX 格式修复下沉到此步骤末尾，用正则自动修复
- 正则修复内容：未闭合的 `$...$`、断裂的 `\frac{}`、裸写的 `<` `>`、裸写的变量名
- 选择题管线：直接输出对错 + 正确选项，不走步骤评分
- 填空题管线：输出答案匹配结果（正确/错误/等价正确），不走步骤评分
- 解答题管线：逐步骤评分，输出每步得分 + 评语
- 非常规题管线：AI 自定评分标准，输出整体评价

---

## 三、知识点共享机制

所有题不管走哪条管线，入库时都打知识点标签。

```json
// 入库后的题目数据示例
{
  "id": 12345,
  "type": "选择题",
  "difficulty": "容易",
  "category": "数列",
  "knowledge_points": ["等差数列", "数列求和"],
  "question_latex": "设{an}为等差数列...",
  "answer": "B",
  "solver_output": { /* ... */ },
  "verifier_output": { /* ... */ }
}
```

**老师搜题时：**

```sql
SELECT * FROM questions WHERE "等差数列" IN knowledge_points;
```

结果同时包含：选择题、填空题、解答题、非常规题——只要标签里有"等差数列"全部出来。

---

## 四、各管线调用情况汇总

| | 选择题 | 填空题 | 解答题 | 非常规题 |
|---|---|---|---|---|
| Step 1 调用模型 | 是 | 是 | 是 | 是 |
| Step 2 调用模型 | 是（轻量） | 是（轻量） | 是（轻量） | 是（轻量） |
| Step 3 调用模型 | 是 | 是 | 是 | 是 |
| Step 4 调用模型 | 否（正则即可） | 否（正则即可） | 是 | 是 |
| 固定步骤表 | 无 | 无 | 有 | 无 |
| formatter 调用 | 已移除 | 已移除 | 已移除 | 已移除 |

---

## 五、与旧架构的对比

| | 旧架构 | 新架构 |
|---|---|---|
| 分类方式 | solver 一把梭 | 切题独立成 Step 2 |
| 题型处理 | 选填共用一个 verifier | 选/填/解/非常规四线分流 |
| formatter | LLM 调用一次 | 正则替代 |
| 知识点来源 | categories.py 写死 | categories.yaml 单一数据源 |
| 非常规题 | 归类为"非常规压轴题"板块 | 独立管线，不受板块约束 |
| 人工介入 | 无接口 | Step 2 结果可人工编辑 |
| 知识标签 | 解题时自动附带 | 所有管线入库时统一打标签 |
