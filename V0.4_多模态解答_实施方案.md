# V0.4 多模态解答 — 实施方案

## 整体架构

```
multimodal.html
┌──────────────────────────────────────────────────────┐
│  上传区（PDF/图片/docx/文本）                          │
├───────────────────────────┬──────────────────────────┤
│  左栏：题目列表（可编辑）    │  右栏：多会话解答区         │
│                           │                          │
│  ┌─ 题1 ──────────────┐  │  ┌─ 会话1 ──────────────┐ │
│  │ ☐ $\\int x^2 dx$   │  │  │ 题目：$\\int x^2 dx$  │ │
│  │    中等 [梁梁]  [▶解]│  │  │ 步骤1/2/3 解答过程    │ │
│  │    [预览]           │  │  │ 难度系数 雷达图       │ │
│  └────────────────────┘  │  │ [重新解题] [保存题库] │ │
│  ┌─ 题2 ──────────────┐  │  └──────────────────────┘ │
│  │ ☐ $\\lim_{x\\to0}$  │  │  ┌─ 会话2 ──────────────┐ │
│  │    困难 [韬韬]  [▶解]│  │  │ 题目：$\\lim$        │ │
│  │    [预览]           │  │  │ [解题中...]  进度条    │ │
│  └────────────────────┘  │  └──────────────────────┘ │
│                           │                          │
│  [批量解题 N 道]          │  右侧每道题独立会话         │
│                           │  并发 7 道，互不阻塞        │
└───────────────────────────┴──────────────────────────┘
```

solver.py 的 step1/2/3 完全不动。只新增切题链路 + 前端多会话管理。

---

## Phase 1：后端 `backend/multimodal.py`（新建）

### 1a. 文件解析层
- PDF → PyMuPDF 逐页转图片
- JPG/PNG → 直接 base64
- docx → python-docx 抽文本
- txt → 直接读
- 输出：`[{ type: "text"|"image", data: string }]`

### 1b. 切题 API
- 调通义千问 Qwen-VL-Plus（阿里云，中国大陆可用，价格低，OCR 强）
- Prompt：逐道提取 → LaTeX 格式 → 初判难度（简单/中等/困难）
- 返回：
```python
[
  {"latex": "...", "difficulty": "中等", "recommended_teacher": "liangliang"},
  ...
]
```

### 1c. 难度→老师映射
- 简单 / 中等 → `liangliang`
- 困难 → `taotao`
- 前端下拉框允许用户覆盖

### 1d. API Key
- `.env` 新增 `DASHSCOPE_API_KEY`（阿里云通义千问）

### 1e. 同步接口
- `POST /multimodal/parse` 同步返回切题结果
- 对当前规模足够，不引入异步 task_id + 轮询的复杂度

---

## Phase 2：`backend/main.py` 加端点

```
POST /multimodal/parse
Content-Type: multipart/form-data
file: <上传的文件>
```
- 调 `multimodal.py` 解析 → 返回 `[{latex, difficulty, recommended_teacher}]`

已有的 solve/step1/2/3 不动。

---

## Phase 3：前端 `frontend/multimodal.html`（新建）

### 3a. 上传区
- 拖拽 + 点击上传
- 支持 .pdf / .jpg / .png / .jpeg / .docx / .txt
- 调 `POST /multimodal/parse`，切题中显示 loading

### 3b. 左栏（题目标签列表）
- 每道题一个卡片，包含：
  - checkbox（批量选）
  - **可编辑 LaTeX** — 点击题目展开 textarea 编辑
  - **[预览] 按钮** — 用 KaTeX 实时渲染 textarea 内容，不用等解题才发现改坏
  - 难度标签 + 老师下拉选择器
  - "▶解题" 按钮 → 在右侧新开会话

### 3c. 右栏（多会话解答区）
- 类似 AI 聊天多会话布局
- 每道题的解答是一个独立的"会话卡片"：
  - 会话标题：题号 + 题目摘要
  - 会话内容：调用 step1/2/3 的解答结果
  - 状态：等待中 / 解题中（进度条）/ 已完成
- 点击左栏题目 → 右侧滚动到对应会话
- 每道题独立进度，互不阻塞

### 3d. 会话内容（复用 index.html 的展示）
- 每个会话渲染的内容 = `displayResult()` 生成的 HTML
- 包括：知识点标签、雪峰/武亮难度系数、雷达图、分块步骤、展开详细过程
- 底部按钮：重新解题、保存到题库

### 3e. 并发控制
- **并发数 7**（一张 19 题的试卷约 3 轮跑完）
- 前端用 `async/await` + 队列管理，一道完成自动启动下一道
- 全部完成后汇总显示总耗时

---

## Phase 4：`index.html` 不改

批量模式不再开新标签页，`index.html` 保持原有功能不变，只改导航。

---

## Phase 5：导航栏统一更新

### 5a. 改名
- 所有页面："和老师对答案" → "单题解答"

### 5b. 加第五项
- 所有页面导航增加多模态入口

---

## 文件清单

| 文件 | 操作 |
|------|------|
| `backend/multimodal.py` | **新建** — 文件解析 + Qwen-VL 切题 |
| `requirements.txt` | **修改** — 加 PyMuPDF、python-docx、通义千问 SDK |
| `backend/main.py` | **修改** — 加 `/multimodal/parse` |
| `frontend/multimodal.html` | **新建** — 左栏编辑预览 + 右栏多会话解答（并发 7） |
| `frontend/style.css` | **修改** — 新页面样式 |
| `frontend/index.html` | **修改** — 导航改名加项 |
| `frontend/history.html` | **修改** — 导航改名加项 |
| `frontend/exam.html` | **修改** — 导航改名加项 |
| `frontend/teach.html` | **修改** — 导航改名加项 |
| `.env` | **修改** — 加 `DASHSCOPE_API_KEY` |
